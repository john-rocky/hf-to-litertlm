# Bonsai — minimal on-device text-to-image app (xcodegen).
# Bundle id deliberately matches BonsaiDeviceTest: same team + same id means
# iOS keeps the Documents container across installs, so the 3.97 GiB model set
# is a one-time copy per device (see device_run.sh for the devicectl command).
# Before generating: ./prep_resources.sh (tokenizer tables + pipeline_meta.json
# into Resources/) and ./prep_clitert.sh (the official CLiteRT.xcframework,
# bazel-built from the LiteRT repo — no LiteRT code is committed here).
name: BonsaiApp
options:
  bundleIdPrefix: com.bonsai
  deploymentTarget:
    iOS: "17.0"
targets:
  BonsaiApp:
    type: application
    platform: iOS
    sources:
      - path: Sources
      - path: ../shared
        group: Shared
      - path: Resources
        buildPhase: resources
    dependencies:
      - framework: CLiteRT.xcframework
        embed: true
        codeSign: true
    info:
      path: Info.plist
      properties:
        CFBundleDisplayName: Bonsai
        UIFileSharingEnabled: true
        LSSupportsOpeningDocumentsInPlace: true
        UILaunchScreen: {}
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bonsai.devicetest
        SWIFT_OBJC_BRIDGING_HEADER: Sources/Bridging.h
        HEADER_SEARCH_PATHS:
          - $(SRCROOT)/../shared
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "1,2"
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: MFN25KNUGJ
        CLANG_CXX_LANGUAGE_STANDARD: c++17
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/Frameworks"
